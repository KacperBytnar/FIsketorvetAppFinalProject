@page
@model FisketorvetWebAppProject.Pages.AllProducts.DeleteProductModel
@{
}
<h2>Are you sure you want to delete this Product?</h2>
<div>
    <form method="post">
        <div class="card-deck" style="text-align:center">

                <div class=" card m-3" style="min-width:18rem;max-width:31%; height:24rem; marxgin-bottom:100px;">
                    <div class="card-header" style="height:15%; text-align:center;padding:0; padding-top:-10px; font-family:'Comic Sans MS'">
                        <p style="text-overflow:initial; text-align:center">
                            @if (Model.Product.Name.Length > 20)
                            {
                                <h5>@Model.Product.Name</h5>
                            }
                            else
                            { <h3>@Model.Product.Name</h3>}

                        </p>
                    </div>
                    <img style="card-img-top: img-thumbnail; max-width:60%; max-height:60%; text-align:center; margin:auto;" src="/images/ProductImages/@Model.Product.Image" alt="Product image">

                    <div class="card-header " style="width:100%; height:10%; padding:0;">
                        <p style="text-overflow:initial; font-size:24px;">
                            <i>Price:</i>  @Model.Product.Price dkk
                        </p>
                    </div>
                    <input type="submit" value="Delete" class="btn btn-danger" />
        </form>
    <div>
        <a asp-page="/AllProducts/AllProducts">Back to Products</a>
    </div>
</div>
